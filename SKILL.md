---
name: fact-check
description: >
  递归声明核查。用户给出一段话/一个说法/一个结论要求判断真假、查证、核实,
  或输出事实性结论之前自查时使用。触发词:核查、验证、查证、是不是真的、
  有几个事实、事实还是观点、帮我查一下这个说法、这段话有问题吗。
  核心:先拆解输入中的全部可验证声明,再逐个递归追依据链到"可指认证据终点或未决",
  逐条汇报状态。禁止只验证其中部分声明就下结论。
---

# 事实核查(递归声明核查)

反幻觉协议。目标:**让「没验证」在输出里无处藏身。**

## 一、先拆解,再核查

顺序是硬的:**先交付声明清单 → 逐条追链 → 汇报**。清单没交付就开始追链 = 犯规。

声明分三类:

- **排除**(不进清单):纯主观评价与偏好、用户自定前提
- **列入但不可核查**(列出、不追链):未来预测、规范应然(无明示标准)、价值判断
- **列入且可核查**(编号、逐条追链):其余全部

拆解检查项——隐含声明就藏在这些地方:

- 数值:两个数字能否相除、单位是否一致、是否线性
- 归因:相关是否被说成因果,方向对不对
- 范围:「所有/大多数/通常」是否量化
- 口径:分母、基准、样本、时间窗口是否一致
- 引用:被当成事实引用的细节是否真的存在
- 转述:二手结论、翻译、他人复述是否保真

拿不准某条算不算关键 → **按关键处理**。边缘声明可以标「未覆盖」,但必须在清单里可见地列出,不许静默丢弃。

## 二、逐条递归追链

每条声明的每个依据,追问它自己的依据,直到终点。验证了 A 不覆盖 B,不许挑声明。

**终点 = 可指认证据终点**:链条在此停止,含义是「到此为止可指认」,**不是「命题在此为真」**。

| 级 | 材料 | 它实际证明什么 |
|---|---|---|
| A | 直接观察、命令在此环境下的实际输出、文件原文(路径:行号) | 一手,可指认、可复现 |
| B | 网页原文(URL + 访问日期)、可追溯的二手记录 | 该来源如此记载 |
| C | 权威统计(口径 + 日期)、转述与翻译 | 某个口径下的二次汇总 |

规则:

- **统计永不为 A**——口径、样本、时间窗都可能错,不发免检牌
- **记忆不是依据**。其他 agent 的结论、他人转述、我上一轮的判断,都只是待核查的声明,不是终点
- 没追到底的依据 = 没验证

## 三、状态与汇报

四个状态,把真值、未定、未覆盖分开:

`✓ 已验证` / `✗ 已证伪` / `? 未决` / `○ 未覆盖`
终端打不出符号时用 `OK / NO / ? / -`。

- **未决必须写明理由**:查无实据 / 来源矛盾 / 循环依据(A 引 B、B 引 A) / 超出能力 / 无权限 / 无数据
- **注解不替代状态**:终点非一手时写「✓ 已验证(依据 B)」,不许光秃秃的 ✓;依赖用户前提的写「✓ 在该前提下成立」
- 未决与未覆盖只能以未定口吻输出,不得升格为事实

```
声明 N: "XXX"
├─ 依据 1: "YYY"
│    └─ 依据 1a: "ZZZ"
│         └─ 终点: A 级(文件 路径:行号 / 命令原文 + 环境)
└─ 依据 2: "WWW"
     └─ 终点: 未决(循环依据:A → B → A)

汇报(逐条,不合并,公示分母):
声明 1  "xxx"    ✓ 已验证(A 级)
声明 2  "yyy"    ? 未决(来源矛盾)
声明 3  "zzz"    ✗ 已证伪(与 文件 路径:行号 冲突)
声明 4  "www"    ○ 未覆盖(边缘声明 / 能力不足)
—— 4 条声明:1 已验证、1 未决、1 已证伪、1 未覆盖
```

**分母必须公示**,禁止「全部已验证」:只有覆盖率 100% 才准用「全部」二字。汇报里每一环都要能指到具体位置(路径:行号 / URL + 日期 / 命令原文 + 环境),否则只是换了个措辞的无依据断言。

时效敏感的结论(统计、价格、版本与 API 行为、在线内容)必须带日期与环境。

## 四、示例(校准粒度)

输入:「某公司去年营收增长 20%,因为用户数翻倍。」

拆解 6 条:①该公司去年营收有增长(存在性)②增幅为 20%(数值)③时间范围是「去年」(时间窗)④用户数翻倍(存在性 + 数值)⑤增长由用户数翻倍导致(因果归因)⑥两个数字口径一致、可作同一基准(隐含前提)

反例:只验证第 ① 条(营收确实增长)就宣布整段可信。

## 五、自查(命中任一条 = 核查无效)

- [ ] 把整段话当成「一个声明」没先拆解,或清单没先交付
- [ ] 只查表面声明,漏了隐含声明(可相除性、单位、量词、分母、基准、时间窗)
- [ ] 把用户输入、他人结论或转述里的细节当事实引用,没查证
- [ ] 只报验证过的那部分,或分母不完整却写「全部已验证」
- [ ] 用「大概」「应该」「我印象中」「基本成立」代替核查或顶替状态
- [ ] 验证了「存在性」就宣布整段话可信
- [ ] 把终点(材料如此记载)当成事实(世界如此)

## 六、两条纪律

- **修正优先于辩解**:发现错误直接改结论,不为旧结论找理由
- 默认 CLI 文本输出;用户要求或结论重要时可另出 HTML 报告,内容仍以证明链为准
